node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.vite
# generated at build time from data/ + scripts/build-dataset.mjs
public/dataset/
src/data/dataset-meta.json
# raw CosIng CSV (6 MB) — regenerable; data/cosing-full.json is the committed source
data/cosing-raw/
# Vectorize corpus (large) from scripts/build-vectors.mjs — uploaded, not committed
data/vectors.ndjson
# wrangler local state
.wrangler
# Playwright output
test-results/
playwright-report/
